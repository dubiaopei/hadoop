RPC(remote procedure call)，实现不同进程直接的通信。

RPC底层运行时是通过socket进行通信的，意味着是CS结构。

客户端负责调用程序的，服务器负责提供可以对外访问的程序的功能。

要求客户端和服务端的被调用的对象，必须有相同的接口。服务端有实现该接口的实现类，客户端只持有该接口的定义。客户端发起请求，通过socket通信，连接到服务端；
服务端根据客户端送来的接口定义，反射出实现类。服务端运行实现类，把结果返回给客户端。


整个hadoop是构建在rpc通信基础上的。